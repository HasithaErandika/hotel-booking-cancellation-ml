# Decision Log

*Rubric alignment: Workflow diagram and decision log (10 marks) — "important
decisions recorded with options, reasons, and evidence."*

Every row here should be added **at the time the decision is made**, not
reconstructed afterward. Format: what we decided, what else we considered, why,
and what evidence supports it. Link to the relevant `docs/` file for full detail.

| # | Decision | Alternatives considered | Choice made | Reason | Evidence | Owner | Date |
|---|---|---|---|---|---|---|---|
| 1 | Business problem / target | Predict ADR/revenue; predict special-requests volume; predict cancellation | Predict `is_canceled` (binary classification) | Directly maps to a recurring operational decision (which bookings need attention); target is clean and unambiguous | `is_canceled` matches `reservation_status ∈ {Canceled, No-Show}` in 100% of 119,390 rows | Team | 2026-09-12 |
| 2 | Unit of analysis | Guest-level; stay-night level; booking-level | One row = one booking | Matches the data's actual grain; a guest can have multiple independent bookings | Data dictionary review, `docs/03-data-understanding.md` §3.1 | Team | 2026-09-12 |
| 3 | Handling of `reservation_status` / `reservation_status_date` | Include as features; include only `reservation_status` category (not date); exclude both | **Exclude both entirely** | Both encode the outcome itself — `reservation_status` matches the target in 100% of rows, so any model using it would trivially "solve" the task by reading the label back, learning nothing usable at real decision time | Direct check: 0 mismatches between `reservation_status`-derived label and `is_canceled` across all 119,390 rows | Bhanuka / confirmed by Hasitha | 2026-09-12 |
| 4 | Exact duplicate rows (31,994 of 119,390, 26.8%) | Keep all rows; deduplicate after train/test split; deduplicate before split | **Deduplicate before the train/test split** | Deduplicating after the split risks the same booking appearing in both train and test, inflating test metrics with memorised rows rather than genuine generalisation | Duplicate count computed directly on the raw CSV (exact match across all 32 columns) | Bhanuka | 2026-09-12 |
| 5 | Missing `agent` (13.69%) / `company` (94.31%) | Drop the columns; drop rows with missing values; mean/mode impute; treat missingness as its own category | **Treat missing as an explicit "no agent / no company" category** | Missingness plausibly means no travel agent/company was involved (informative), not a random gap; dropping `company` rows would discard 94% of the dataset | Missingness % computed directly; 9,760 rows are missing both simultaneously | Bhanuka | 2026-09-12 |
| 6 | `assigned_room_type` as a predictive feature | Use directly as a feature; drop entirely; keep only an engineered boolean flag, used cautiously | **Keep only as an engineered `room_type_changed` flag, treated as exploratory, excluded from the "day-zero" model variant** | Room reassignment often happens close to arrival/check-in, so using it for an early cancellation-risk score is optimistic about what's actually known at booking time | 14,917 of 119,390 rows (12.49%) show `reserved_room_type != assigned_room_type` | Seneja | 2026-09-12 |
| 7 | Class imbalance (62.96% / 37.04%) | Full SMOTE oversampling to 50/50; undersampling; `class_weight`/`scale_pos_weight`; do nothing | **`class_weight="balanced"` (sklearn models) / `scale_pos_weight` (XGBoost) as default; SMOTE kept as a documented comparison, not default** | 37% positive class is a moderate, not severe, imbalance — aggressive resampling risks distorting the natural data distribution more than needed; weighting is simpler and leakage-safe by construction | Target distribution computed directly: 75,166 / 44,224 | Seneja | 2026-09-12 |
| 8 | Extreme `adr` values (max 5,400; 1 negative value; 1,959 zeros) | Delete all extreme rows; leave untouched; cap/winsorize | **Correct the single negative value; winsorize the 5,400 outlier at a high percentile rather than delete; leave zero-ADR rows as-is (likely legitimate comped stays)** | A single implausible value shouldn't cost a whole row of otherwise valid data, but shouldn't be allowed to dominate scaling either; zero ADR overlaps plausibly with `market_segment = Complementary` | Direct inspection: max-ADR row is a genuine City Hotel booking (2 adults) that was in fact cancelled, not an obvious encoding glitch | Seneja | 2026-09-12 |
| 9 | Model portfolio | Only Logistic Regression + one tree model; add a neural network; add k-NN/Naive Bayes | **Dummy baseline + Logistic Regression + Decision Tree + Random Forest + XGBoost (later extended to 6 models with SVM, see decision 18)** | Exceeds rubric's "baseline + 3 alternatives"; covers interpretable linear, rule-based, ensemble, and boosting approaches without adding a neural network that isn't justified by dataset size/structure | `docs/05-modeling-strategy.md` §5.1–§5.2 | Team | 2026-09-12 |
| 10 | Primary ranking metric | Accuracy; F1; ROC-AUC; PR-AUC | **PR-AUC primary, reported alongside precision/recall/F1/ROC-AUC/confusion matrix — never accuracy alone** | Naive majority-class baseline already reaches 62.96% accuracy while catching zero cancellations, making accuracy alone misleading under this imbalance; PR-AUC is more informative than ROC-AUC for the minority (cancelled) class | Baseline accuracy computed directly from target distribution | Hasitha | 2026-09-12 |
| 11 | `deposit_type = Non Refund` anomaly (99.36% cancellation rate) | Report as a straightforward causal driver; exclude from the model as "too good to be true"; include, but flag as a probable process/channel confound, not a causal deposit effect | **Include as a feature, explicitly flagged in the report as a likely confound rather than a simple causal story** | A near-deterministic 99.36% cancellation rate for one category is very unusual and is documented in this dataset's known literature as likely reflecting a specific booking channel/process convention, not "customers with non-refundable deposits decide to cancel more" | Direct group-by: 14,494 of 14,587 Non-Refund bookings (99.36%) are cancelled, vs. 28.38% for No Deposit | Hasitha | 2026-09-12 |
| 12 | Two model variants: "day-zero" vs. "full-lifecycle" | Only build one variant using every available column | **Train and report both variants explicitly** | Makes the leakage-timing trade-off (whether to use post-booking fields like `booking_changes`/`days_in_waiting_list`) a visible, evidence-based decision rather than an invisible default | `docs/04-preprocessing-feature-engineering.md` §4.6, `docs/05-modeling-strategy.md` §5.3 | Team | 2026-09-12 |
| 13 | Team structure / model ownership | Split strictly by pipeline stage only (EDA / features / models / report as 4 silos) | **Shared data contract + shared preprocessing pipeline; each member also owns one model end-to-end** | Prevents the common group-project failure of 4 members training on 4 different versions of the data; still gives every member a substantial, attributable technical contribution | `docs/02-workflow-and-architecture.md` §2.3, `docs/team/roles-and-raci.md` | Team | 2026-09-12 |
| 14 | Random seed | Different seed per member/model; no fixed seed | **`RANDOM_STATE = 42` fixed project-wide** (split, CV folds, all model seeds) | Required for reproducibility and for fair, comparable evaluation across models trained by different people | `docs/08-reproducibility.md` §8.2 | Team | 2026-09-12 |
| 15 | Train / Dev / Test split ratio | 80/10/10; 60/20/20; no separate Dev set (CV only) | **70% Train / 15% Dev / 15% Test**, stratified on `is_canceled` | ~87,396 unique rows after dedup is large enough that 15/15 (≈13K rows each) is statistically stable, and the larger Dev slice absorbs repeated peeking during tuning across 6 models without needing to touch Test more than once | Row-count math in `docs/04-preprocessing-feature-engineering.md` §4.10 | Hasitha | 2026-09-12 |
| 16 | Order of split vs. cleaning steps | Fit preprocessing on the full dataset, then split; split first, then fit on train only | **Fixed/rule-based cleaning (leakage-column drop, dedup, hard value fixes) before the split; all statistics-dependent preprocessing (imputers, encoders, scalers, outlier caps) fit strictly after the split, on Train only** | Fitting any learned statistic on the full dataset before splitting leaks test-set information into training; deduplication specifically must happen pre-split or a duplicate pair can span both splits | `docs/04-preprocessing-feature-engineering.md` §4.10 | Hasitha | 2026-09-12 |
| 17 | Threshold selection split | Sweep thresholds on Test; sweep on Dev | **Threshold and calibration analysis performed on Dev only; Test is scored exactly once with the threshold already locked in** | Choosing a threshold by looking at Test performance turns Test into a second Dev set through repeated peeking, biasing the final reported metrics optimistically | `docs/06-evaluation-plan.md` §6.5 | Hasitha | 2026-09-12 |
| 18 | 6th model / SVM addition | Keep 5 models (1 baseline + 4); add a 6th only if time allows | **Add SVM (RBF kernel) as a 6th model, owned by Hasitha** | Enforces the non-negotiable team rule that every member personally trains at least one model end-to-end — Hasitha's role was evaluation-only, which didn't independently satisfy that rule | `docs/05-modeling-strategy.md` §5.0, §5.1 | Team | 2026-09-12 |
| 19 | Neural network (Deep NN / shallow NN / SNN) for Member 4 or as a general model | Use a Deep NN or SNN as Member 4's model instead of SVM; add a shallow NN as a 7th model | **Rejected all three; SVM remains Member 4's model** | ~61,177 Train rows is enough to *train* a NN without erroring, but not enough for a deep architecture's extra capacity to be repaid in accuracy over XGBoost/Random Forest on this kind of mixed categorical/continuous tabular data (Shwartz-Ziv & Armon 2021; Grinsztajn et al. 2022); a NN would also need entity embeddings for high-cardinality categoricals (`country`, `agent`) and its own early-stopping loop, and is harder to explain to a business stakeholder than SVM/trees — a bad trade against the rubric's interpretability/responsible-AI weighting. SNN's specific benefit (stabilising very deep, ~8+ layer, purely-continuous networks) doesn't match this dataset's shape at all | `docs/05-modeling-strategy.md` §5.2 | Hasitha | 2026-09-12 |
| 20 | Target leakage removal: `reservation_status` & `reservation_status_date` | Retain as features; use one-hot encoded statuses; drop post-booking leakage columns | **Drop `reservation_status` and `reservation_status_date` across all models** | EDA confirmed a 100% deterministic correlation with `is_canceled` (Check-Out = 0; Canceled/No-Show = 1), constituting severe target leakage that invalidates real-world inference at booking time | EDA crosstab proof in `notebooks/01_data_quality_eda.ipynb` and assertion check in `src/data/validation.py` | Bhanuka | 2026-09-15 |

## How to add a new decision

Append a new row with the next number. Keep the "Evidence" column concrete
(a computed number, a specific check, a citation) — "it seemed reasonable" is not
evidence. If a later decision reverses an earlier one, don't delete the old row;
add a new row referencing the old decision number and explaining what changed and
why.
